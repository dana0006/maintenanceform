services:
  - type: web
    name: odoo-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerCommand: odoo -c /etc/odoo/odoo.conf --without-demo=all
    envVars:
      - key: PORT
        value: 10000